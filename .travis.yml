language: go

go:
    - 1.8.x
    - master

install:
    - go get -d -v ./...

script:
    - go build -v
    - test -z "$(gofmt -s -l . | grep -v vendor | tee /dev/stderr)"
    - go vet ./...
    - go test -v ./...

after_success:
    - go test -bench=.
